import { addDays, format } from "date-fns";

export const MOCK_TYPE_OF_TRAININGS = [
	{ type: "running", amount: Math.floor(Math.random() * 100) },
	{ type: "weightlifting", amount: Math.floor(Math.random() * 100) },
	{ type: "swimming", amount: Math.floor(Math.random() * 100) },
];
